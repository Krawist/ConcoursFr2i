<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activities.CameraActivity">

    <FrameLayout
        android:id="@+id/surfaceview_camera_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <FrameLayout
        android:id="@+id/button_capture_image"
        android:layout_width="75dp"
        android:layout_height="75dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:padding="@dimen/semi_fab_margin"
        android:background="@drawable/camera_button_parent_background"
        android:layout_margin="@dimen/fab_margin">

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/camera_button_background"/>

    </FrameLayout>

    <ImageButton
        android:id="@+id/button_flash"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/semi_fab_margin"
        app:layout_constraintBottom_toBottomOf="@id/button_capture_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/button_capture_image"
        app:layout_constraintTop_toTopOf="@id/button_capture_image"
        android:src="@drawable/ic_round_flash_on_24"
        android:background="@drawable/button_round_background"/>

    <ImageButton
        android:id="@+id/button_camera_sense"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/semi_fab_margin"
        app:layout_constraintBottom_toBottomOf="@id/button_capture_image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/button_capture_image"
        app:layout_constraintTop_toTopOf="@id/button_capture_image"
        android:src="@drawable/ic_outline_flip_camera_ios_24"
        android:background="@drawable/button_round_background"/>

</androidx.constraintlayout.widget.ConstraintLayout>